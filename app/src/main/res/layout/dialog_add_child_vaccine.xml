<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- ── Dialog Container ── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:orientation="vertical"
        android:padding="20dp"
        android:backgroundTint="@android:color/white"
        tools:ignore="UnusedAttribute">

        <!-- Note: Ensure your parent dialog or window sets rounded corners,
             or wrap this inside a CardView for easy corner radiuses. -->

        <!-- ── Header Section ───────────────────────────────────── -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp">

            <!-- Close Button Icon background -->
            <androidx.cardview.widget.CardView
                android:id="@+id/btnDialogClose"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_alignParentStart="true"
                android:layout_centerInParent="true"
                app:cardBackgroundColor="#F0F2F5"
                app:cardCornerRadius="10dp"
                app:cardElevation="0dp">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_close"
                    app:tint="#5F6368" />
            </androidx.cardview.widget.CardView>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginStart="14dp"
                android:layout_toEndOf="@id/btnDialogClose"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Select Child"
                    android:textColor="#111B21"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Choose which child this vaccine is for"
                    android:textColor="#8696A0"
                    android:textSize="12sp" />
            </LinearLayout>
        </RelativeLayout>

        <!-- ── Dynamic Children List ───────────────────────────── -->
        <!-- Using a RecyclerView is best practice to dynamically load children -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvChildrenSelection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:overScrollMode="never"
            tools:itemCount="2"
            tools:listitem="@layout/item_dialog_child_select" />

        <!-- ── Action Button ───────────────────────────────────── -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnContinue"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:text="Continue"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="#009688"
            app:cornerRadius="16dp"
            app:icon="@drawable/ic_chevron_right"
            app:iconGravity="textEnd"
            app:iconPadding="8dp"
            app:iconTint="#FFFFFF" />

    </LinearLayout>
</LinearLayout>